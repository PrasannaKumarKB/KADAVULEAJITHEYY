// import { Routes, RouterModule } from '@angular/router';
// import { HomeComponent } from './home/home.component';
// import { InternalRegistrationComponent } from './internalregistration.component';
// import { StatusComponent } from './status/status.component';

// const routes: Routes = [
//   { path: '', component: HomeComponent },  // Home route
//   { path: 'register/internal', component: InternalRegistrationComponent },  // Internal Registration route
//   { path: 'status', component: StatusComponent },  // Registration Status route
//   { path: '**', redirectTo: '', pathMatch: 'full' }  // Fallback to home route if no match
// ];

// export const AppRoutingModule = RouterModule.forRoot(routes);
